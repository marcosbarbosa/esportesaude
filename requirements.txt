# --- Interface e Dados ---
streamlit==1.57.0
pandas
plotly
streamlit-keyup
streamlit-sortables

# --- Integração e Nuvem ---
supabase
requests
httpx

# --- Inteligência Artificial ---
google-genai

# --- Geração de Documentos (Excel, Word, PDF) ---
xlsxwriter
python-docx==0.8.11
lxml==4.9.4
# xhtml2pdf fixado em 0.2.11 + svglib<1.6 para evitar o build nativo do pycairo
# (svglib>=1.6 puxa rlpycairo -> pycairo, que falha sem cairo/pkg-config no Nix).
# Os relatórios PDF usam HTML/CSS puro + imagens base64, nunca SVG, portanto cairo é desnecessário.
xhtml2pdf==0.2.11
svglib<1.6
reportlab
fpdf2

# --- Manipulação de Imagens e Suporte ---
Pillow
python-bidi

# --- Reconhecimento Facial (DeepFace) ---
deepface==0.0.90
tf-keras==2.16.0
tensorflow-cpu==2.16.1
opencv-python-headless==4.9.0.80
docx
fpdf
matplotlib